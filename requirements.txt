# Core async / DB
asyncpg==0.30.0
asyncio-mqtt  # keep if used elsewhere

# LLM providers
openai==1.90.0
google-generativeai

# Knowledge graph
graphiti-core==0.12.4
neo4j==5.28.1

# PostgreSQL / vector
# pgvector is handled server-side; asyncpg handles the client

# Ingestion / ML
tiktoken
numpy>=1.24.0

# Config / validation
pydantic==2.11.7
pydantic-settings==2.10.0
python-dotenv==1.1.0

# Dashboard
streamlit>=1.30.0
plotly>=5.18.0
nest_asyncio>=1.6.0   # ADDED: fixes asyncio.run inside Streamlit event loop
pandas>=2.0.0

# Testing
pytest==8.4.1
pytest-asyncio==1.0.0

# Misc utilities already in env (pin only what matters)
httpx==0.28.1
tenacity
PyYAML