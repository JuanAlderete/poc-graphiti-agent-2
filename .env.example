# ─── LLM Provider ────────────────────────────────────────────────────────
# Options: openai | gemini | ollama
LLM_PROVIDER=ollama

# ─── API Keys ────────────────────────────────────────────────────────────
# Only needed for the provider you're using:
OPENAI_API_KEY=ollama
GEMINI_API_KEY=

# ─── Models ──────────────────────────────────────────────────────────────
# Ollama defaults (auto-configured if blank):
DEFAULT_MODEL=qwen2.5:3b
EMBEDDING_MODEL=nomic-embed-text

# OpenAI defaults:
# DEFAULT_MODEL=gpt-5-mini
# EMBEDDING_MODEL=text-embedding-3-small

# ─── Ollama Base URL ─────────────────────────────────────────────────────
# Local (without Docker):
# OPENAI_BASE_URL=http://localhost:11434/v1
# Docker (connects to host Ollama):
OPENAI_BASE_URL=http://host.docker.internal:11434/v1

# ─── Neo4j ───────────────────────────────────────────────────────────────
# Docker service name as host:
NEO4J_URI=neo4j://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=adminadmin

# ─── PostgreSQL ──────────────────────────────────────────────────────────
# Docker service name as host:
POSTGRES_USER=postgres
POSTGRES_PASSWORD=admin123
POSTGRES_DB=graphiti_poc
POSTGRES_HOST=postgres
POSTGRES_PORT=5432